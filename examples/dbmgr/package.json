{
  "name": "dbmgr-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The dbmgr console. Runs standalone and is embedded into the dbmgr Go binary — the second real consumer of the showcase package.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json --threshold error"
  }
}
